#+ endpoint: http://ecaickathon-api.tech:3030/default/sparql
#+ method: GET
#+ summary: Number of triples per graph
#+ tags: 
#+   - metadata 

SELECT ?graph (COUNT(*) AS ?triples) WHERE { 
	GRAPH ?graph {
		?s ?p ?o.
	}
}
